# BIDR Relight


`other_code` will be temporary directory for holding other people code that could be useful (but not used yet)

`test` will hold all the playground notebooks for testing stuffs. 


## TODO
Didn't touch app.py, 



## Installation
```
python -m venv .venv
source .venv/bin/activate 
pip install -e .
```

## Scripts
Generate `.tiff` images from RAW format such as DNG or CR2
```
python scripts/process_raw.py --path <folder_path with cr2/dng>
```


